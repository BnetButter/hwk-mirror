from pathlib import Path
import os

MENUPATH = os.path.join(str(Path.home()), ".hwk", "menu_items.json")
LOGPATH = os.path.join(str(Path.home()), ".hwk", "log.txt")
CONFIGPATH = os.path.join(str(Path.home()), ".hwk", "config.json")